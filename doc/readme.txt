c:\terrysql 을 만들고 여기에서 create-ract-app을통하여 sql db를 연동하는 프로젝트

c:\terrysql npm install -g create-react-app (create-react-app을 global로 설치)
c:\terrysql create-react-app dbdb  (dbdb 디렉토리가 생기면서 project 를 만든다)
c:\terrysql\dbdb\ 에 ...
public, src 디렉토리가 만들어져 있다.
public은 그대로 두고 src는 우리스타일에 맞게 바꾸겠다.

node_modules\react-scripts\script\ ... 참조

- src 디렉토리는 client 로 이름을 바꾼다.
=
- server directory를 만든다.(terrydb의 mvc를 따라갈 것이다)
